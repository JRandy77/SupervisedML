// marp.config.js
module.exports = {
  // tell Marp where your custom theme(s) live
  themeSet: [
    './themes/micm_theme.css'
  ],
  // default options for every run
  options: {
    theme: 'micm_theme',
    allowLocalFiles: true
  }
};
